[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0", "wheel"]

[project]
dependencies = [
  "fastmcp>=3.4.3,<4",
  "fastapi>=0.115.0",
  "slack-sdk>=3.33.5",
  "python-dotenv>=1.0.0",
]
description = "Slack MCP Server with OAuth authentication"
name = "slack-mcp"
readme = "README.md"
requires-python = ">=3.10"
version = "1.0.0"

[[project.authors]]
name = "Aaron Wang"

[project.license]
text = "Apache-2.0"

[project.scripts]
slack-mcp = "server:main"

[tool.setuptools]
packages = ["auth"]
py-modules = ["server", "slack_tools"]

[project.optional-dependencies]
dev = ["ruff>=0.12.4"]
test = ["pytest>=8.0.0"]
